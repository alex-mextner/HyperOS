# Import note

This directory is being populated from the unpacked `AgentOS-Engineering-Bible-Foundation-2026-07-12.zip` baseline. The source archive contains 370 files. The current branch is incomplete until its file inventory and checksums reconcile with `validation/FILE-MANIFEST.csv` and `validation/CHECKSUMS.sha256` from the baseline.
