function api(){nav('api');app.innerHTML=`${workspaceHead('Stable public interfaces','API and datasets','The portal is a projection over Git-hosted Markdown, machine-readable planning data and GitHub work records.')}<div class="api-grid"><a class="api-card" href="${raw('data/index.json')}"><span class="method">GET JSON</span><h3>Task catalog manifest</h3><p>Nine phase shards covering all 327 work items.</p></a><a class="api-card" href="${raw('data/docs-index.json')}"><span class="method">GET JSON</span><h3>Wiki document index</h3><p>Titles, categories, status and canonical AOS identifiers. When absent, the portal derives the index from the repository tree.</p></a><a class="api-card" href="https://api.github.com/repos/${REPO}/issues"><span class="method">GET REST</span><h3>GitHub Issues</h3><p>Live mutable tasks, discussions, assignments and status history.</p></a><a class="api-card" href="${gh('/actions')}"><span class="method">ACTIONS</span><h3>Automation</h3><p>Validation, task import, derived data and publication workflows.</p></a><a class="api-card" href="${gh('/tree/main/engineering-bible/docs')}"><span class="method">MARKDOWN</span><h3>Canonical Wiki source</h3><p>Editable Markdown with stable IDs, anchors and cross-links.</p></a><a class="api-card" href="${gh('/issues/21')}"><span class="method">PLANNED</span><h3>Project v2 write adapter</h3><p>Least-privilege GitHub App for dates, fields, hierarchy and dependencies.</p></a></div>`}
function route(){const parts=routeParts(),view=parts[0]||'home';if(view==='build')buildLane();else if(view==='developers')developersLane();else if(view==='investors')investorsLane();else if(view==='users')usersLane();else if(view==='vision')home();else if(view==='compare')compare();else if(view==='devices')devices();else if(view==='landscape')landscape();else if(view==='wiki')wiki(parts[1],parts[2]);else if(view==='tasks')tasks(parts[1]);else if(view==='gantt')gantt(parts[1]);else if(view==='claims')claimsView();else if(view==='experiments')experimentsView();else if(view==='sources')sourcesView();else if(view==='risks')risksView();else if(view==='glossary')glossaryView();else if(view==='decisions')decisionsView();else if(view==='procurement')procurement();else if(view==='contacts')contacts();else if(view==='api')api();else audienceHome();scrollTo(0,0)}
function openModal(html){modalBody.innerHTML=html;modal.classList.add('open')}function closeModal(){modal.classList.remove('open')}modal.onclick=e=>{if(e.target===modal)closeModal()};addEventListener('hashchange',route);boot();
