(()=>{const header=document.querySelector('.site-header'),toggle=document.querySelector('.menu-toggle'),current=document.querySelector('.current-section');const links=[...document.querySelectorAll('[data-route]')];const groups=[...document.querySelectorAll('.nav-group')];const canonical=v=>({gantt:'roadmap',home:'',wiki:'wiki'}[v]??v);function activeView(){const p=typeof routeParts==='function'?routeParts():['home'];return canonical(p[0]||'home')}function sync(){const view=activeView();let label='Home';links.forEach(a=>{const on=(a.dataset.route||'')===view;a.classList.toggle('on',on);if(on&&a.dataset.label)label=a.dataset.label});groups.forEach(g=>g.classList.toggle('on',!!g.querySelector('.on')));if(current)current.textContent=label;document.title=label==='Home'?'Agent OS':`${label} · Agent OS`}function closeAll(){header?.classList.remove('menu-open');toggle?.setAttribute('aria-expanded','false');groups.forEach(g=>g.classList.remove('open'))}function navigate(path){history.pushState({},'',path||'/');closeAll();route();sync();scrollTo(0,0)}document.addEventListener('click',e=>{const a=e.target.closest('a[data-route]');if(a&&a.origin===location.origin){e.preventDefault();navigate(a.getAttribute('href'))}const trigger=e.target.closest('.nav-trigger');if(trigger){const group=trigger.closest('.nav-group'),was=group.classList.contains('open');groups.forEach(g=>g.classList.remove('open'));group.classList.toggle('open',!was);trigger.setAttribute('aria-expanded',String(!was))}else if(!e.target.closest('.nav-group'))groups.forEach(g=>g.classList.remove('open'))});toggle?.addEventListener('click',()=>{const open=!header.classList.contains('menu-open');header.classList.toggle('menu-open',open);toggle.setAttribute('aria-expanded',String(open));toggle.innerHTML=open?'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>':'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 7h16M4 12h16M4 17h16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>'});addEventListener('popstate',()=>{route();sync()});addEventListener('hashchange',sync);addEventListener('keydown',e=>{if(e.key==='Escape')closeAll()});const observer=new MutationObserver(sync);observer.observe(document.querySelector('#app'),{childList:true,subtree:false});sync()})();